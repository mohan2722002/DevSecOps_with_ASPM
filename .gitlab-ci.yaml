stages:
  - build

variables:
  DOCKER_TLS_CERTDIR: ""

before_script:
  - docker info

build:
  stage: build
  tags:
    - centos  # Make sure your local GitLab runner is tagged this way
  script:
    - echo "🚧 Building containers using Docker Compose..."
    - docker compose -f .build/docker-compose.yml up -d --no-cache

